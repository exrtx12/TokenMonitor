# 의존성 — npm install 로 복원
node_modules/

# 빌드 산출물 — exe 한 개가 80MB가 넘습니다.
# 배포용 파일은 GitHub Releases 에 올립니다.
dist/

# 실행 중 생기는 것들
*.log
npm-debug.log*

# OS
Thumbs.db
.DS_Store

# 탐색 도구가 덤프한 네트워크 기록 (응답 본문에 계정 정보가 섞일 수 있음)
desktop/tools/out/
